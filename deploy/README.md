# Deploy Profiles

Deployment packaging is deferred to a later reviewed milestone.

The planned profiles are:

- `local-full`: local development and evaluation.
- `hosted-demo`: synthetic-data-only constrained demonstration.
- `live-disabled`: official provider adapters disabled by default.

None of these profiles is implemented in the foundation. External side effects remain disabled through application configuration.
